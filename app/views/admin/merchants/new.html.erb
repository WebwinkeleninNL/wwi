<h3>New Merchant</h3>

<%= form_for  @merchant, :url => admin_merchants_path(@merchant)  do |form| %>
  <%= render :partial => '/admin/merchants/form', :locals => {:form => form} %>
  <%= form.submit "Create", :class => 'button' %>
  <%= link_to 'Cancle', admin_merchants_path %>
<% end %>
